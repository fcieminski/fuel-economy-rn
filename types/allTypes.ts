export type Fuelling = {
  distance: number;
  cost: number;
  fuelAmount: number;
  date: string;
  timestamp: number;
};

export type Car = {
  brand: string;
  model: string;
  engine: string;
  mileage: number;
};
